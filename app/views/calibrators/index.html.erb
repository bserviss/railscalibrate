<h1>Vendors</h1>

<div class="digg_pagination">
    <div clas="page_info">
      <%= page_entries_info @calibrators %>
    </div>
    <%= will_paginate @calibrators, :container => false %>
</div>

<%= render :partial => 'calibrators', :collection => @calibrators %>

<div class="digg_pagination">
    <div clas="page_info">
      <%= page_entries_info @calibrators %>
    </div>
    <%= will_paginate @calibrators, :container => false %>
</div>

<br />

<%= link_to 'New vendor', new_calibrator_path %>