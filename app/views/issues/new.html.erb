<h1>New Issue for <%= "#{h(@item.description)}"%> </h1>

<% if @issue.errors.any? %>
<h2 style="color: red"><%= pluralize(@issue.errors.count, "error") %> prohibited this event from being saved:</h2>
<ul>
<% @issue.errors.full_messages.each do |msg| %>
  <li style="color: red"><%= msg %></li>
<% end %>
</ul>
<% end %>

<%= form_for([ @item, @issue ]) do |f| %>
  <%= f.hidden_field :item_id %>
<table>
  <tr>
    <td><%=f.label :description%></td>
    <td><%=f.text_field :description %></td>
  </tr>
</table>
<p>
  <%= f.submit 'Create' %>
</p>
<% end %>

<p><%= link_to 'Back to issue', item_issues_path( @item.id ) %></p>
<p><%= link_to "Back to #{h(@item.description)}", item_path( @item ) %></p>
<p><%= link_to 'Back to Summary Page', items_path %></p>
